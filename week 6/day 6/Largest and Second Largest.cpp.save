#include<bits/stdc++.h>
using namespace std;

int main()
{
	int t; cin>>t; 
	while(t--)
	{
		int n; cin>>n;
		map<int , int>mp;
		for(int i=0; i<n; i++ ) 
		{
			int x; cin>>x;
			mp[x] = i;
		}
		auto it=mp.end();
		it--;
		int element=it->first;
		it--;
		int e2 = it->first;
		int e = element + e2;
		cout<<e<<endl;
	}
}