#include<bits/stdc++.h>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string s; cin>>s;
    int fre[26];
    int sz = s.size();
    for(int i=0; i<sz; i++)
    {
		fre[s[i] - 'a']++;
    }
    int e=0, o = 0;
    for(int i=0; i<26; i++)
    {
		if(fre[i]>)
		if(fre[i] % 2 == 0) e++;
		else o++;
    }
    if(o>1) cout<<"NO\n";
    else cout<<"YES\n";
    
}