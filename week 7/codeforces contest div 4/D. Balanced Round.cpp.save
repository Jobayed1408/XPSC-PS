#include<bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
	ll t; cin>>t;
	while(t--)
	{
		ll n , k; cin>>n >> k;
		ll arr[n];
		for(int i=0; i<n; i++ ) cin>>arr[i];
		sort(arr, arr+n);
		
		int c= 0, ans = 0;
		
		for(int i=1; i<n; i++)
		{
			if( abs(arr[i] - arr[i-1]) <=k )
			{
				c++;
				
			}
		}
	}
}