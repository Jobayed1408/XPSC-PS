#include<bits/stdc++.h>
using namespace std;

int main()
{
	int t; cin>>t;
	while(t--)
	{
		int n; cin>>n;
		int arr[n];
		for(int i=0; i<n; i++) 
		{
			cin>>arr[i];
		}
		vector<int>v;
		bool falg = true;
		for(int i=0; i<n; i++)
		{
			for(int j=0; j<n; j++)
			{
				if(a[j]%a[i] == 0) v.push_back(a[j]);
				else v.puah_back(0);
			}
			for(int k=1; k<v.size(); k++)
			{
				if(a[k] == a[k-1]) flag = false;
			}
		}
		if(flag ) cout<< 
		
	}
}