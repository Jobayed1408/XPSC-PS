#include<bits/stdc++.h>
using namespace std;

int main()
{
	int t; cin>>t;
	while(t--)
	{
		int l ,r; cin>>l >> r;
		int max = 0, min = 10;
		
		for(int i=l ; i<r; i++)
		{
			string s = to_string(i);
			sort( i.begin(), i.end() );
			int cur = i[i.length() - 1]  - i[0];
			
			if(max<cur){
				 max=cur;
				 ans = i;
			}
			
			if(max == 9) breakl
			
		}
	}
}