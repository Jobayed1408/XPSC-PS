#include<bits/stdc++.h>
using namespace std;
 
int main()
{
 int t; cin>> t;
 while(t--)
 {
  int n; cin>> n;
  int ar[n];
  int mi = INT_MAX;
  int ma = INT_MIN;
  for(int i=0; i<n ; i++) 
  {
   cin >> ar[i];
   mi = min(mi , ar[i]);
   ma = max(ma , ar[i]);
  }
  cout << ma - mi << endl;
 }
}